import { 
    SUBIR_ARCHIVO_EXITO,
    SUBIR_ARCHIVO_ERROR,
    CREAR_ENLACE_EXITO,
    CREAR_ENLACE_ERROR,
    LIMPIAR_ALETRTA
} from "@/types";

export default (state, action) => {
    switch (action.types){

        

        default:
            return state
    }
}